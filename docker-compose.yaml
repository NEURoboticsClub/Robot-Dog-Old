version: '3.7'

services:
  ros-master:
    image: ros:noetic-ros-core
    command: roscore
    # will make network.gethostname() use the address of raspberi pi not container
    network_mode: host 
  test-pub:
    build:
      context: ./test_pub
      dockerfile: Dockerfile
    environment:
      - "ROS_MASTER_URI=http://localhost:11311"
    command: bash -c "source /opt/ros/noetic/setup.bash && python3 test_pub.py"
    depends_on:
      - ros-master
    network_mode: host

  server:
    build:
      context: ./test_server_sub
      dockerfile: Dockerfile
    environment:
      - "ROS_MASTER_URI=http://localhost:11311"
    command: bash -c "source /opt/ros/noetic/setup.bash && python3 test_server_sub.py"
    depends_on:
      - ros-master
    network_mode: host
